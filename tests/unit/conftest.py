"""Unit test fixtures for pytest-uuid.

Unit tests verify individual components in isolation without running
pytest subprocesses. The mock_uuid, spy_uuid, and mock_uuid_factory
fixtures are provided by the pytest_uuid plugin itself.
"""
